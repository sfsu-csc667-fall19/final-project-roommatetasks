export const setNotes = notes => ({
    type: 'SET_NOTES',
    notes,
});

export const setTryNotes = tryNotes => ({
    type: 'SET_TRY_NOTES',
    tryNotes,
});

export const setDoneNotes = doneNotes => ({
    type: 'SET_DONE_NOTES',
    doneNotes,
});